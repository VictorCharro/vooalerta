{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "buildCommand": "node scripts/generate-env.js && PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install chromium && npm run build:prod",
  "outputDirectory": "dist/vooalerta/browser",
  "installCommand": "npm install",
  "framework": "angular",
  "env": {
    "PLAYWRIGHT_BROWSERS_PATH": "0"
  }
}
